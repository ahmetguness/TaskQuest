import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: {
    flex: 1,
  },
  textContainer: {
    justifyContent: "center",
    alignItems: "center",
    marginTop: "30%",
  },
  text: {
    fontWeight: "bold",
    fontSize: 24,
    marginBottom: "20%"
  },
});
